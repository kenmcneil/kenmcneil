spring:
  application:
    # Name is used to lookup configuration on the configuration server and for service discovery.
    name: api-task-campaign-application # participation-processor
  profiles:
    active: localdev
  cloud:
    config:
      #The label/branch that will be used to retrieve configuration from the configuration server.
      label: SODEV-24977
      fail-fast: true
      uri: http://config
      username: config
      password: Cheve1le!

---

spring:
  profiles: local,dev,localdev,test
  cloud:
    config:
      fail-fast: true

---

spring:
  profiles: production
  cloud:
    config:
      fail-fast: true
      username: sdc
